
--PROJECTION

--Select the id, type, amount, unit, accountNum, country, exchange from the create transaction table
--SELECT id, type, amount, unit, accountNum, country, exchange FROM CreatesTransaction;

SELECT id, type, amount, units, accountNum, country, exchange 
FROM CreatesTransaction;

